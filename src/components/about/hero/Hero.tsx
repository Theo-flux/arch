'use client';
import React from 'react';
import { Section, DivTag } from '@/shared';

function Hero() {
  return (
    <Section>
      <DivTag>About Hero</DivTag>
    </Section>
  );
}

export default Hero;
